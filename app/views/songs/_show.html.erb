<% @songs.each do |song, id| %>
  <tr>
    <td><%= song.name %></td>
    <td><%= link_to "Delete", "/songs/#{song.id}/delete", remote: true, class: "btn btn-danger" %></td>
  </tr>
<% end %>



<%# artist_id = @artist.id %>
    <%# song_id = song.id %>
<!-- <td><%#= link_to "Delete", song_delete_path(song.id), remote: true, class: "btn btn-danger" %></td> -->
<!-- <td><%#= link_to "Delete", "/artists/#{@artist.id}/songs/#{song.id}/delete", remote: true, class: "btn btn-danger" %></td> -->
